# TODO

## Architecture

- [ ] `bridge-outbound` depends on `@nostr-mail-bridge/core` only for `runPlugin`. Consider extracting the plugin system into a separate package `@nostr-mail-bridge/plugin` to avoid this coupling.
